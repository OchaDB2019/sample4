html
    head
        link(ref="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons")
        link(rel="stylesheet" href="css/materialize.min.css" media="screen,projection")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
    body
        script(type="text/javascript" src="js/materialize.min.js")
        div(class="container")
            h5 #{count} tweets in your timeline.
            each row in timeline
                div(class="tweet")
                    div(class="tweetinfo") #{row['name']} @#{row['account']} #{row['datetime']}
                    div(class="row")
                        div(class="col s10 offset-s1 content") #{row['content']}

        